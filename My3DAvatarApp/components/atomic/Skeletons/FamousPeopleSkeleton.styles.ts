import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    padding: 10,
    height: 50,
  },
  itemWrapper: {
    borderRadius: 5,
    overflow: "hidden",
  },
});
